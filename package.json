{
  "name": "html-template",
  "version": "1.0.0",
  "main": "index.js",
  "description": "Full HTML + CSS + TailwindCSS + JavaScript template with auto-reload via CDN-injected scripts",
  "scripts": {
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.6.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
